#!/bin/sh

echo "\033[1;33m15. Find the command that kills the background command’s process.\033[0m"
	echo "kill %1"
read -p "" ; ./16

# Use fg to brig up all current jobs and choose which to kill.
